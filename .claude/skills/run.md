---
name: run
description: Start the Clique dev server using Docker at http://localhost:3000
user-invocable: true
---

Start the Clique application using Docker (PostgreSQL + app with hot reload).

Run the following bash command in the background:

```bash
npm run docker:dev
```

After starting, tell the user the app is available at http://localhost:3000.
